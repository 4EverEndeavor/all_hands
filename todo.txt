1. Add a feature to main_agent.py so the initial agent prompt tries to read the contents of a file called AGENTS.md. This file contains any additional context that applies only to the current directory.
2. Colorize the terminal output.

